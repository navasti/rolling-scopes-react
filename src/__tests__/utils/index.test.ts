import { capitalize, uuid, getTodayDate } from 'utils';

describe('General utils', () => {
  it('Capitalize first letter', () => {});
  it('Generate UUID', () => {});
  it('Get today date', () => {});
});
