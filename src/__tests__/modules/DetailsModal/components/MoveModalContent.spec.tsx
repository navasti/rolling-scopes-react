import { MoveModalContent } from 'modules/DetailsModal/components';
import { screen, render } from '@testing-library/react';

describe('', () => {
  it('', () => {
    render(<MoveModalContent selectedMove={} />);
  });
});
