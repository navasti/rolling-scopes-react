import { screen, render } from '@testing-library/react';
import { DetailsModal } from 'modules';

jest.mock('components', () => ({
  __esModule: true,
  Modal: () => <></>,
}));

describe('', () => {
  it('', () => {});
});
