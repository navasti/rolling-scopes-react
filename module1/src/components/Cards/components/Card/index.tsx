import React from 'react';
import * as Styled from './styled';

// interface Props {}
// interface State {}

export class Card extends React.Component<unknown, unknown> {
  // constructor(props: Props) {
  //   super(props);
  // }
  render() {
    return <Styled.Card></Styled.Card>;
  }
}
